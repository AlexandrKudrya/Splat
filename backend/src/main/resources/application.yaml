spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/merilo
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: supersecretkey_32chars_minimum!!
  expiration-minutes: 1440

s3:
  endpoint: http://localhost:9000
  access-key: minio
  secret-key: minio123
  bucket: merilo

telegram:
  bot-token: "8340785841:AAHZGvwO5qyt_8iQj_drFyDQEmNSbAafHG4"
security:
  initdata-max-age-seconds: 86400

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.springframework.boot.web.servlet.error: DEBUG

server:
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: always